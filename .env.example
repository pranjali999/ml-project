# Flask API (see backend/README.md)
# Default is fine: in `npm run dev` the app uses same-origin `/api` (Vite proxy → :8000).
# Set a full URL only if you need to bypass the proxy (e.g. test from another device).
VITE_API_BASE_URL=http://127.0.0.1:8000
